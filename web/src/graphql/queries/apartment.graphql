query Apartment($apartmentId: Int!) {
    apartment(id: $apartmentId) {
        name
        description
        floor
        areaSize
        price
        numberOfRooms
        address
        isRented
    }
}
